# Environment — never commit secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# Project data (SQLite DBs, OAuth tokens, traces)
data/
*.db
*.sqlite3

# Playwright (project-local browsers + artifacts)
.playwright-browsers/
demo/recordings/

# Demo media — kept local only (large binaries; screenshots show a demo account email)
docs/demo/screenshots/
docs/demo/video/
docs/demo/*.aac

# Claude local files
.claude/settings.local.json
CLAUDE.local.md

# Internal working notes / private prep (session state, review prompts, cheat sheet)
docs/plans/

# OS
Thumbs.db
.DS_Store
